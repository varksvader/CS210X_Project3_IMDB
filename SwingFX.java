import javafx.application.Application;
import javafx.stage.Stage;

/**
 * TODO: delete this later
 * this allows u to run JavaFX applications in eclipse without downloading
 * ec(fx)lipse or whatever is is
 * additionally, you need to open up properties for the project, build path -> jre
 * then go to the asset labelled rules and add a new rule with allowing javafx/**
 * or something like that its on canvas
 *
 */
public class SwingFX extends Application {
	
	@Override
	public void start(Stage stage){
		// ...
	}
	
	public static void main(String[] args){
		launch(args);
	}
}
